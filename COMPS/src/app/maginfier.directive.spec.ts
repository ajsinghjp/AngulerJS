import { MaginfierDirective } from './maginfier.directive';

describe('MaginfierDirective', () => {
  it('should create an instance', () => {
    const directive = new MaginfierDirective();
    expect(directive).toBeTruthy();
  });
});
